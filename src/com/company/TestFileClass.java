package com.company;
import java.io.File;
public class TestFileClass {

    /*

    public  static void printDirContents(File dir){
        //print name of directory followed by DIR
        System.out.println(dir.getName()+" DIR ");

        File [] files = dir.listFiles() ;   // retrive content as array of files

        if(files != null )
        for (int i = 0 ; i < files.length ; i++ ){
            if(files[i].isFile())     //if object retrived is a file return its name followed by size
                System.out.println(files[i].getName() + files[i].length());
            else
                printDirContents(files[i]); // recursive call for the subdirectory

        }


    }



     */

}
